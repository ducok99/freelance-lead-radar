# Dependencies & build
node_modules/
dist/
coverage/
.turbo/
.wrangler/

# Secrets — KHÔNG BAO GIỜ commit (SECURITY.md §6)
.dev.vars
.env
.env.*

# Playwright (từ Phase 5)
playwright-report/
test-results/

# Bản vá giao qua chat (chỉ là file giao, không phải source)
*.patch

# Hệ điều hành / editor
.DS_Store
*.log
