# @flr/dashboard — Web dashboard cho team (GIAI ĐOẠN 2)

Giữ chỗ theo kiến trúc monorepo. KHÔNG phát triển trong MVP — dashboard MVP là trang nằm trong extension (ARCHITECTURE.md §4.6, ADR-09). Package này chỉ được bắt đầu ở phase G2-1 sau khi DUC duyệt riêng.
